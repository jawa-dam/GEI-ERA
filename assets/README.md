# GEI-ERA Brand Assets

- `yalltoo-logo.svg` — responsive wrapper around the canonical YallToo logo source.
- `yalltoo-logo-source.txt` — canonical source URL and supplied dimensions.

The final production build should replace the remote-image SVG wrapper with a repository-hosted binary asset so the brand remains available if the source host changes.
