# Next Architecture Step

V1.0.11 should introduce module-owned action adapters so commands can invoke richer actions while each module retains ownership of its state, validation, permissions, payment verification, research integrity, and credentials.
