# UI note

The homepage remains dark-first. The light theme tokens and persistent controller are staged as part of V0.1.2 so the next Control Room UI pass can wire the toggle into the primary navigation without changing the brand system.
