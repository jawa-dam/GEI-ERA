# V1.0.4 Implementation Notes

The Progress Center is read-oriented over existing local state. It adds its own achievement unlock timestamps but does not replace or mutate the source engines.

Academy remains an explicit optional adapter until a dedicated Academy progress source is available.
