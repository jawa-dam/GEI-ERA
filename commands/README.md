# GEI Command Center

The `/commands/` surface provides the V1.0.10 bounded command orchestration experience.

Commands resolve against `command-data.js` and dispatch only to registered GEI modules. The command layer does not own protected state, payments, credentials, identity, or research authority.
